name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    name: Build and publish
    runs-on: windows-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Run tests
        run: go test ./...

      - name: Build release binaries
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Path dist -Force | Out-Null

          $targets = @(
            @{ Arch = "amd64"; Output = "nosleep-windows-amd64.exe" },
            @{ Arch = "arm64"; Output = "nosleep-windows-arm64.exe" }
          )

          foreach ($target in $targets) {
            $env:GOOS = "windows"
            $env:GOARCH = $target.Arch
            go build -trimpath -ldflags="-s -w -X nosleep-cli/internal/cli.Version=$env:GITHUB_REF_NAME" -o "dist/$($target.Output)" ./cmd/nosleep
          }

          Get-ChildItem dist/*.exe | ForEach-Object {
            $hash = (Get-FileHash -Algorithm SHA256 -LiteralPath $_.FullName).Hash.ToLowerInvariant()
            "$hash  $($_.Name)"
          } | Set-Content -Encoding ascii dist/checksums.txt

          Copy-Item install.ps1 dist/install.ps1
          Copy-Item uninstall.ps1 dist/uninstall.ps1

      - name: Publish GitHub release
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $files = Get-ChildItem dist | ForEach-Object { $_.FullName }

          gh release create $env:GITHUB_REF_NAME @files `
            --verify-tag `
            --title "NoSleep CLI $env:GITHUB_REF_NAME" `
            --notes "Windows binaries, checksums, installer, and uninstaller are attached to this release. Run install.ps1 to install or update the matching binary for your system, or download a binary manually and place it in a directory on your User Path."
